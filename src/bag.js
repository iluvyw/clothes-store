export function BagItem(id, price, name, brand, slug, imageUrl, number){
    return {
        id: id,
        price: price,
        name: name,
        brand: brand,
        slug: slug,
        imageUrl: imageUrl,
        number: number
    }
}