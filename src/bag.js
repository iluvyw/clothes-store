export function BagItem(id, name, brand, slug, imageUrl, number){
    return {
        id: id,
        name: name,
        brand: brand,
        slug: slug,
        imageUrl: imageUrl,
        number: number
    }
}